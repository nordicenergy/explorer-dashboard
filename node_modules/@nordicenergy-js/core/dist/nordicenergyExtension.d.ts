import { HttpProvider, WSProvider, Messenger, ShardingItem } from '@NordicEnergy-js/network';
import { Transaction, TransactionFactory } from '@NordicEnergy-js/transaction';
import { Blockchain } from '@nordicenergy-js/core/dist/blockchain';
import { ContractFactory } from '@NordicEnergy-js/contract';
import { NordicEnergyConfig } from '@nordicenergy-js/core/dist/util';
export declare enum ExtensionType {
    MathWallet = "MathWallet"
}
export interface ExtensionAccount {
    address: 'string';
    name: 'string';
}
export interface ExtensionNetwork {
    chain_url: string;
    net_version: number;
}
export interface ExtensionInterface {
    signTransaction: (transaction: Transaction, updateNonce: boolean, encodeMode: string, blockNumber: string) => Promise<Transaction>;
    getAccount: () => Promise<ExtensionAccount>;
    messenger?: Messenger;
    version: string;
    isMathWallet?: boolean;
    network: ExtensionNetwork;
}
export declare class NordicEnergyExtension {
    extensionType: ExtensionType | null;
    wallet: ExtensionInterface;
    provider: HttpProvider | WSProvider;
    messenger: Messenger;
    blockchain: Blockchain;
    transactions: TransactionFactory;
    contracts: ContractFactory;
    crypto: any;
    utils: any;
    defaultShardID?: number;
    constructor(wallet: ExtensionInterface, config?: NordicEnergyConfig);
    setProvider(provider: string | HttpProvider | WSProvider): void;
    setShardID(shardID: number): void;
    isExtension(wallet: ExtensionInterface): void;
    login(): Promise<ExtensionAccount>;
    shardingStructures(shardingStructures: ShardingItem[]): void;
    private setMessenger;
}
//# sourceMappingURL=NordicEnergyExtension.d.ts.map